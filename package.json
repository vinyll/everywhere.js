{
  "name": "everywhere.js",
  "version": "1.0.0",
  "scripts": {
    "babelify": "./node_modules/.bin/babel js/everywhere.js -o dist/everywhere.js",
    "minify": "./node_modules/.bin/uglifyjs dist/everywhere.js -o dist/everywhere.min.js",
    "build": "npm run babelify && npm run minify"
  },
  "devDependencies": {
    "babel-cli": "^6.0",
    "babel-preset-es2015": "^6.0",
    "uglify-js": "^2.7"
  },
  "dependencies": {}
}
